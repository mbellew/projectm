# Example inventory. bootstrap.sh targets localhost over a local connection, so you normally
# don't need this file. Use it only if you want to apply the playbook to a remote appliance:
#
#   ansible-playbook -i inventory.ini playbook.yml
#
[appliances]
my-projectm-box ansible_host=192.168.1.50 ansible_user=admin
