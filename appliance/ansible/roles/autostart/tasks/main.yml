---
# Dispatch to the per-OS implementation. ansible_system is "Darwin" on macOS, "Linux" on Linux.
- name: "autostart: apply {{ ansible_system }} tasks"
  ansible.builtin.include_tasks: "{{ ansible_system }}.yml"
